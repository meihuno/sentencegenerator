package nlp.sample.sentencegenerator;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

/**
 * NGramのテストクラス。
 * テストメソッドは未実装（各スムージングクラスで動作確認している）
 * */
public class TrialTest {

	@Test
	public void test() {
		boolean flag = true;
		assertEquals(flag, true);		
	}

}
